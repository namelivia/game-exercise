name: Typecheck

on: [push, pull_request]

jobs:
  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
      - uses: jpetrucciani/mypy-check@0991
        with:
          mypy_flags: "--config-file .mypy.ini --strict"
